/* Purpose: After grabbing the input from the use
 * generate a sine wave of the desired frequency 
 * and hopefully with the help of an "envelope"
 * replicate to the best of my ability the sound
 * of a real grand piano.
 */
/*
use std::io::{stdin, stdout, Write};
use std::error::Error;
//use std::thread;
use midir::{MidiInput, Ignore};
use wmidi::MidiMessage::{self, *};
use pitch_calc::Step;
use dimensioned::si;
use synth::Synth;
*/
pub fn generate_sound(){


}


